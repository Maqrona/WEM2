name: Action Test

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  print-hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: execute echo
        run: echo "Hello Github Actions!"